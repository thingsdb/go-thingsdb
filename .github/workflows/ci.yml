name: CI
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go 1.21
        uses: actions/setup-go@v6
        with:
          go-version: 1.21
      - name: Run tests
        env:
          TI_TOKEN: ${{ secrets.TI_TOKEN }}
        run: go test -v ./...